<%= form_with(model: [ @case, @case.case_comments.build ]) do |f| %>
  <p>
    <%= f.text_area :body %>
  </p>
    <%= f.hidden_field :user_id, value: current_user.id %>
  <p>
    <%= f.submit %>
  </p>
<% end %>
